# {{PROJECT_NAME}} Specification

{{ONE_LINE_DESCRIPTION}}

This specification is a **knowledge base, not a single document**. It is
organised for progressive disclosure: start here, descend into the categories
you care about, and follow cross-links to the reference appendices when you need
precise contracts. Every file is self-contained and language-agnostic — logic is
expressed as pseudocode and diagrams, not code in any one language.

## How to read this spec

<!-- plan-1-specify will add reader-intent entry points here as categories grow,
     e.g. "I want to understand what this is and why → read 01-foundations/." -->

## Categories

<!-- One row per top-level category directory. plan-1-specify maintains this table.
     Keep it at category granularity (index scope rule): describe direct children
     only, one line each. Do not list individual files here — they are found via
     search against frontmatter. -->

_No categories yet. Run `plan-1-specify` to author the first one._

## Reference

- [`reference/`](reference/index.md) — glossary and architecture decision records.
