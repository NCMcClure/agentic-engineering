---
tags: [plan, status, board]
summary: "Live status of the plan tree — epics, sprints, and issues with statuses, blockers, acceptance progress, and the next unblocked work."
created: {{MONTH}}
updated: {{MONTH}}
---

# Plan

<div id="plan-view">
  <p><strong>Live plan status isn't available in this static view.</strong></p>
  <p>This page renders the plan tree (<code>.plan/plan/</code>) — epics, sprints,
  and issues with statuses, blockers, and the next unblocked issue — when the
  site is served by the comments server. Start it via the
  <code>/but-first-planning:spec-open</code> skill, or run
  <code>python .plan/spec/scripts/comments-server.py</code> from the repo root,
  then reload this page.</p>
  <p>The plan tree itself is always browsable on disk under
  <code>.plan/plan/</code>.</p>
</div>
