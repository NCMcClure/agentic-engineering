# Prototypes

Throwaway HTML design artifacts — **not product code**. Each prototype is one
self-contained static `.html` file in this directory, served verbatim by the
spec site and linked (or iframed) from the spec pages it illustrates. Start a
new one by copying [`prototype-skeleton.html`](prototype-skeleton.html).

<!-- One line per prototype, maintained by spec-1-specify:
- [login flow](login-flow.html) — the three-screen login/reset path (spec: NN-ui/key-screens.md) -->

_No prototypes yet._
